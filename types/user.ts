// types/user.ts
export interface User {
    username: string;
    proof: any;  // Define a more specific type based on your actual proof object
    publicSignals: any[]; // Again, specify this with a more precise type if possible
}
  